{% extends "NzoTunisiefretBundle::layoutClient.html.twig" %}
{% block title %} {{ parent() }} Client {% endblock %}
{% block banner %}
<div class="jumbotron">
    <div class="col-md-4"> 
        <div class="row">
            <h4 class="title-banner" style="">Détails Demandes Export </h4>
        </div>
    </div>
</div>
{% endblock banner %}
{% block content %}
<div class="col-md-8 col-md-offset-2 well">
    <h4>{{mydemande.titre}} </h4>
    <h4>{{mydemande.reference}} </h4>
    <h4>{{mydemande.prix}} </h4>
    <h4>{{mydemande.description}} </h4>
    <h4>{{mydemande.pays}} </h4>
    <h4>{{mydemande.ville}} </h4>
    <h4>{{mydemande.codepostal}} </h4>
    <h4>{{mydemande.adresse}} </h4>
    <h4>{{mydemande.datemax|date("d/m/Y")}} </h4>
    <h4>{{mydemande.datedepos|date("d/m/Y")}} </h4>
    
    
    
    <hr>
Liste Postule: <br>

{% for postule in postules %} 
    {{postule.id}} | Date Postule: {{postule.datepostule|date("d/m/Y")}} | <a href="{{path('nzo_detaildemande_export_postule', {'id':postule.id} )}}"> Voir </a>
    <br>
{% endfor %}
</div> 
{% endblock content %}
 
 